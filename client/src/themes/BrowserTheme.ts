import { createTheme } from "@mui/material";

const browserTheme = createTheme({
    palette:{
        primary: {
            main: '#fff'
        },
        secondary: {
            main: "#fff"
        }
    }
})
export default browserTheme